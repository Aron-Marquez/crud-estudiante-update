import { NgModule } from '@angular/core';
import { DropdownModule } from 'primeng/dropdown';

@NgModule({
  imports: [
    // Otros imports
    DropdownModule,  // Importa el módulo del Dropdown
  ],
})
export class AppModule {}
